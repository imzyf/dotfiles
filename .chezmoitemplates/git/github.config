{{- $upstream := includeTemplate "git/upstream.github.config" . -}}
{{- if not (regexMatch `\A\[user\]\n` $upstream) }}{{ fail "git/upstream.github.config no longer starts with [user]; update the override in git/github.config" }}{{ end -}}
[user]
	email = 9289792+imzyf@users.noreply.github.com
	name = Yifans_Z

{{ regexReplaceAllLiteral `(?s)\A\[user\]\n[^\[]*` $upstream "" -}}
